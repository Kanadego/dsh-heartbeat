# v1.6.4

**修复：设置卡「闲着模式」开关导致 Web 界面加载失败**

v1.6.3 新增的「闲着模式」开关在设置卡渲染时有一个语法错误，会导致基于脚本加载的客户端 bundle 无法注册、Web 界面报「Failed to load plugins」而无法进入。本版本修复该处语法错误，设置卡可正常渲染，插件可正常加载。

- 修复 `client.js` 中设置卡渲染的语法错误（一行补全缺失的标点）。
- 功能与 v1.6.3 完全一致，无行为变更。

安装/升级方式不变（npm 或 GitHub release），升级后建议刷新一次浏览器。
